package com.msc.memories.model;

public enum Role {
	USER,  // Event Host / Photographer: Can create events, upload images, manage their own data
    ADMIN  // System Administrator: Full access across all tables and operations

}
